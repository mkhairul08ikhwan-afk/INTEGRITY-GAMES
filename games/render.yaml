services:
  - type: web
    name: integrity-hunting
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && gunicorn -w 1 -k gthread --threads 8 -b 0.0.0.0:$PORT app:app
